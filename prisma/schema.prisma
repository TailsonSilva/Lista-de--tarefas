generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Tasks { 
  id        Int     @id @default(autoincrement())
  text      String 
  completed Boolean @default(false)
}